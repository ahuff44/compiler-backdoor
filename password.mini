def verify_user(username, password) {
  if (username == "ksmith" &&
      password == "yellowdart") {
    return true;
  } else {
    return false;
  }
}

# Get user credentials:
do _print("username =?");
let username = _input();
do _print("password =?");
let password = _input();
do _print("");

# Check credentials:
if (verify_user(username, password)) {
  do _print("Access granted");
} else {
  do _print("***Access denied***
    This incident will be reported.");
}
